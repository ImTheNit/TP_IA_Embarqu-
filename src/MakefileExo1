CC = gcc
SRC = Exo1.c
TARGET = exo1

TARGETDIR = ../bin
OUT = $(TARGETDIR)/$(TARGET)

#all: $(OUT)

$(OUT): $(SRC)
	@mkdir -p $(TARGETDIR)
	$(CC) -o $@ $<

clean:
	rm -f $(OUT)

fclean: clean
	rm -rf $(TARGETDIR)

